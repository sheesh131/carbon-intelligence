# Model Registry

Optional local model registry artifacts.

- `registry.json`: model metadata registry
- `credit_risk_rf/`: legacy stored model example

Not required for current API runtime unless explicitly wired.
